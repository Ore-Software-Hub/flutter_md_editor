name: md_editor
description: "A Markdown Editor with a toolbar builtin"
version: 0.0.1
homepage: https://labs.oresoftware.net/
repository: https://github.com/Ore-Software-Hub/md_editor

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
